'use strict';

angular.module('primApp')
    .factory('Grup', function ($resource, DateUtils) {
        return $resource('api/grups/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
